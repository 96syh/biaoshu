"""Prompt helpers split from app.utils.prompt_manager."""
from __future__ import annotations

import json
from typing import Any


def _json(data: Any, *, indent: int | None = None) -> str:
    return json.dumps(data, ensure_ascii=False, indent=indent, separators=None if indent else (",", ":"))


def _schema_contract(schema_name: str, schema_json: str, include_schema: bool) -> str:
    if include_schema:
        return f"JSON schema：\n{schema_json}"
    return f"输出必须符合 {schema_name} 结构；结构由 API response_format/json_schema 约束。"


def get_full_bid_rulebook() -> str:
    """跨行业标书风控规则，供所有阶段复用。"""
    return """标书生成通用规则（GPT-5.5 精简版）：

目标与优先级：
1. 以招标文件和澄清/补遗为最高依据；样例、历史案例和通用经验只能补结构、语气和表达，不得覆盖本项目硬要求。
2. 冲突时按“招标文件 > 澄清/补遗 > 企业已提供资料 > 企业知识库/历史材料 > 通用经验”处理。
3. 所有关键结论应可追溯到条款、章节、表格、页码、短原文或企业资料来源。

禁止虚构：
1. 不得编造企业名称、资质、业绩、人员、金额、日期、证书编号、合同编号、发票、社保、信用查询、联系方式、报价、税率、期限或承诺。
2. 缺失或未核验信息用占位符：〖待补充：资料名称〗、〖待确认：事项〗、〖待提供扫描件：资料名称〗、〖待提供查询截图：事项〗、〖以招标文件要求为准〗、〖页码待编排〗。
3. 不得把“不满足、未提供、待确认、未核验”写成“满足、已具备、已完成、已提供”。

生成对象边界：
1. “投标文件/投标文件格式/投标文件组成/编制要求/方案应包括”是目录、正文和审校硬约束。
2. 先定位 selected_generation_target：full_bid 生成完整卷册；technical_only、technical_service_plan、service_plan 或存在明确方案 target 时，只生成对应方案分册/章节。
3. 方案分册不得混入投标函、报价、保证金、资格审查资料等完整投标文件正文；这些内容只保留为排除项、材料项或审校提示。
4. 同一生成对象出现在多个章节时合并理解：组成章节确认对象，格式章节提取目录/表格/签章要求，评分章节补充响应重点。

格式、报价与暗标：
1. 固定表格、投标函、报价表、授权委托书、承诺函、偏离表、资格表、人员表、业绩表等不得改变表头、列名、固定文字、行列结构和签章栏。
2. “★、*、▲、实质性、不允许偏离、投标无效、废标、否决、必须、不得、应当”等内容必须单独识别、响应和审校。
3. 报价卷按招标文件规定的币种、税率、口径、小数位、限价、分项、算术修正规则处理；未提供报价时只输出 〖待补充：报价金额/分项报价〗。
4. 技术/商务/暗标卷如要求隔离价格或身份信息，正文不得出现报价、企业名称、人员姓名、业绩名称、联系方式、Logo、商标、可识别图片或异常页眉页脚。

证据链与评分响应：
1. 业绩、人员、资质、信用、货物、工程、服务证据均需区分“要求提供”和“已提供材料”；仅有历史文字不得认定为有效附件。
2. 技术/服务/实施方案围绕评分细则逐条展开；高分、实质性和易失分项获得更高目录层级、更细正文和更明确材料占位。
3. 同级章节职责清楚，避免重复生成同质化段落。

输出：
1. 要求 JSON 时，只输出合法 JSON，不输出 markdown 代码块、解释语或前后缀。
2. 要求正文时，只输出当前章节正文，不输出标题、AI 自述或生成过程。
3. 页码、附件页码、响应页码统一使用 〖页码待编排〗。
"""
